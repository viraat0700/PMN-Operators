apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafanadashboards
  namespace: pmn
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs

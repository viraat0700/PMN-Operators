apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafanadatasources
  namespace: pmn
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
